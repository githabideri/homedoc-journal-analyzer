# Example environment configuration for HomeDoc — Journal Analyzer
# Duplicate this file as `.env` and adjust values to suit your environment.

HOMEDOC_SERVER=http://localhost:11434/api/generate
HOMEDOC_MODEL=gemma3:4b

# Optional: scope of log scan and entry guardrails
HOMEDOC_LAST=24h
HOMEDOC_MAX_ENTRIES=10000

# Optional: OpenWebUI hand-off
HOMEDOC_OPENWEBUI_BASE=https://openwebui.example.com
HOMEDOC_OPENWEBUI_TOKEN=changeme
